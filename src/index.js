import './css/styles.scss';
import './components/AppCard/AppCard';
/*
const Movies = new MovieList(mcuList);
let cardStack = new CardStack(Movies.getComparison(),
  document.querySelector('#challenger'), document.querySelector('#defender'));

document.querySelector('#challenger').addEventListener('finished', () => {
  Movies.finishComparison();
  if (Movies.finished) {
    document.querySelector('#conclusion').innerHTML = Movies.Movies.join('<br />');
    document.querySelector('.viewport').classList.add('flipped');
  } else {
    cardStack = new CardStack(Movies.getComparison(),
      document.querySelector('#challenger'), document.querySelector('#defender'));
  }
});
*/
